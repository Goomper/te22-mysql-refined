<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    {% block content %}
    <p>{{ message }}</p>
    <ul>
        <li>
            <p>{{ bird.species }}</p>
            <p>{{ bird.name }}</p>
            <p>{{ bird.wingspan }}</p>
        </li>
    </ul>
    {% endblock %}
</body>
</html>