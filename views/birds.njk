<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    {% block content %}
    <p>{{ message }}</p>
    <ul>
        {% for item in birds %}
            <li>
                <p>{{ item.id }}, {{ item.species }}, {{ item.name }}</p>
            </li>
        {% endfor %}
    </ul>
    {% endblock %}
</body>
</html>